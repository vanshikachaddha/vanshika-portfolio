import React, { useEffect, useRef } from 'react'

const AnimatedBackground = () => {
  // Background is now handled per section with different colors
  // No need for animated canvas background
  return null
}

export default AnimatedBackground

